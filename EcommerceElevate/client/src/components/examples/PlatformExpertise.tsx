import { PlatformExpertise } from '../PlatformExpertise';
import { LanguageProvider } from '../LanguageProvider';

export default function PlatformExpertiseExample() {
  return (
    <LanguageProvider>
      <PlatformExpertise />
    </LanguageProvider>
  );
}
